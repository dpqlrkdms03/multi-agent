# 11주차 실습 기록

## GitHub 저장소

- URL:

## 내 에이전트 주제

- 에이전트 이름: Assignment Priority Agent
- 해결하려는 문제: 여러 과제의 마감일(D-Day)을 비교하여 어떤 과제를 먼저 수행해야 하는지 우선순위를 정리
- 입력: 과제명과 D-Day(남은 일수)가 포함된 멀티라인 문자열(`SAMPLE_INPUT`)
- 출력: 우선순위별 과제 목록(Markdown 스타일)과 간단한 안내문(터미널 출력)

## 일정공지 예시에서 바꾼 점

| 일정공지 예시                 | 내 에이전트                                                    |
| ----------------------------- | -------------------------------------------------------------- |
| 공지문                        | 과제 목록(과제명 + D-Day)                                      |
| 제목, 대상, 날짜, 준비물 추출 | 과제명과 남은 일수(D-Day) 추출                                 |
| 학년별 분류                   | 긴급/보통/여유 기준의 우선순위 분류                            |
| 사용자별 안내문               | 우선순위별 간단 안내문(즉시착수/우선순위정하기/계획적으로진행) |

## 사용한 도구

- 로컬 편집기 및 Python(직접 실행)

## 내가 준 주요 프롬프트

- `SAMPLE_INPUT` 문자열을 사용하여 `extract_facts`, `classify_items`, `write_output` 세 함수로 역할을 분리한 `my_agent.py`를 만들어 달라고 지시. 외부 패키지·API 사용 금지, 각 단계의 중간 결과를 `print`로 출력하도록 요청.

## 코딩에이전트가 확인한 항목

| 확인 항목                                    | 통과 여부 | 근거                                                                                                                            |
| -------------------------------------------- | --------: | ------------------------------------------------------------------------------------------------------------------------------- |
| 함수가 3개 역할로 나뉘었는가                 |      통과 | `my_agent.py`에 `extract_facts`, `classify_items`, `write_output` 함수가 각각 구현되어 있음                                     |
| 앞 단계 출력이 다음 단계 입력으로 들어가는가 |      통과 | `main()`에서 `facts = extract_facts(lines)` → `grouped = classify_items(facts)` → `outputs = write_output(grouped)` 흐름 확인됨 |
| 중간 결과가 출력되는가                       |      통과 | `=== facts ===`, `=== classified ===`, `=== output (priority guide) ===` 출력 코드가 있어 중간 결과가 터미널에 표시됨           |

## 실행 결과

- 실행 명령: `python my_agent.py`
- 성공 여부: 성공
- 오류가 있었다면 오류 내용: 없음

## 12주차에 고칠 점

- [ ] 핵심 정보 추출 정확도 향상: `extract_facts()` 정규식 보완, `YYYY-MM-DD` 등 다른 날짜 표기 인식
- [ ] 분류 기준 안정화: 경계값과 범위 표기(예: `3~4학년`) 처리 규칙 정리
- [ ] 결과 파일 출력 추가: `output.md`로 그룹별 Markdown 저장 기능 추가
- [ ] 외부 도구 검토: Groq API 등 외부 NLP 도구 도입 타당성 평가
- [ ] 불확실성 처리 정책 마련: 입력에 없는 정보는 자동 추정하지 않고 `확인 필요`로 표기

## 12주차에 붙여 보고 싶은 확장

- Groq API 사용 여부: 결정 필요(검토 예정)
- 외부 도구/API 사용 여부: 현재 미사용(우선은 Python 표준 코드 유지)
