# Week 09 수업 실습: 학과 공지 정리 설계

## Step 1. 예시 과제 이해하기

```markdown
- 도메인: 학과 공지 정리
- 작업: 여러 공지에서 학생 유형별 행동 목록 만들기
- 이 작업이 반복되는 상황: 수강 신청, 장학금, 졸업 요건, 교환학생 신청 공지가 동시에 올라올 때
- 최종 사용자가 원하는 결과: 1학년, 4학년, 복수전공생, 교환학생 준비생별로 해야 할 일과 마감일을 바로 확인한다
```

---

## Step 2. 단일 에이전트로 부족한 이유 쓰기

### 문제점

1. **정보 혼재**: 공지의 사실 추출(제목, 대상, 마감일), 학생 유형 분류, 최종 안내문 작성이 한 응답에 섞인다. 특히 대상이 "1~3학년 및 복수전공생"일 때 누가 신청 대상인지 모호할 수 있다.

2. **검증 약화**: 마감일 추출 오류나 대상 학년 오독을 스스로 검증하지 못한다. 예를 들어 "4월 30일 오후 5시까지"라는 문장에서 시간대를 연속성 있게 추출해야 하는데, 한 에이전트가 모두 담당하면 일부만 뽑을 수 있다.

3. **사람 검증 부하 증가**: 학생에게 안내하기 전, 마감일 해석과 신청 조건이 원문과 일치하는지 사람이 매번 원문과 대조해야 한다.

---

## Step 3. 역할 나누기

| 역할 | 입력 | 작업 | 출력 | 검증 기준 |
|---|---|---|---|---|
| **공지 수집자** (Fact Extractor) | 공지 원문 | 제목, 대상, 마감일, 준비물, 안내 링크 추출 | 공지별 구조화된 사실 목록 | 각 항목에 원문 출처가 있는가? 필수 항목이 누락되지 않았는가? |
| **대상 분류자** (Classifier) | 공지별 사실 목록 + 학생 유형 정의 | 각 공지가 어떤 학생 유형에 해당하는지 분류 | 학생 유형별 관련 공지 후보 목록 | 대상 조건을 정확히 읽었는가? 예외 조건(예: 제외 대상)을 놓쳤는가? |
| **안내 작성자** (Guide Writer) | 학생 유형별 후보 공지 목록 | 각 학생 유형이 바로 읽을 수 있는 행동 안내문 작성 (어떻게, 언제, 위험한 점) | 학생 유형별 할 일 목록 (Markdown 형식) | 행동 항목이 명확한가? 마감일과 링크가 포함됐는가? 원문에 없는 추측이 없는가? |
| **검토자** (Reviewer) | 최종 안내문 + 원본 사실 목록 + 원본 공지 원문 | 빠진 조건, 오류, 위험한 표현, 원문과의 불일치 확인 | 수정 요청 또는 승인 여부 + 구체적 수정 사항 | 원문에 없는 내용이 추가되지 않았는가? 마감일이 정확한가? 권장 + 필수 항목 구분이 명확한가? |

---

## Step 4. 흐름과 상태 설계하기

### 처리 흐름

```
[사용자 입력: 공지 여러 개]
   ↓
[공지 수집자]
   - 각 공지에서 제목, 대상, 마감일, 준비물, 링크 추출
   - 구조화된 사실 표 생성
   ↓
[대상 분류자]
   - 학생 유형 정의 (1학년, 4학년, 복수전공생, 교환학생 준비생)
   - 각 공지를 해당 학생 유형별로 분류
   - 유형별 후보 공지 목록 생성
   ↓
[안내 작성자]
   - 각 학생 유형별로 해야 할 일 정리
   - "~해야 한다", "~에 주의하세요" 형식의 안내문 작성
   - Markdown 형식의 최종 안내문 생성
   ↓
[검토자]
   - 원본 공지와 대조해 오류 확인
   - 위험 표현 검출 (예: "해야 한다"는데 원문에 없는 경우)
   - 수정 요청 또는 승인
   ↓
[사람 승인]
   - 검토자 피드백 검토
   - 마감일·신청 자격·링크 최종 확인
   ↓
[최종 출력]
   - 학생 유형별 할 일 목록 (공지 채널, 학과 홈페이지 등에 게시)
```

### 공유 상태 (Shared State)

```python
class NoticeState:
    """공지 정리 프로세스의 공유 상태"""
    
    user_request: str
    # 사용자가 원하는 정리 형식 (예: "1학년, 4학년, 복수전공생별로")
    
    notice_sources: List[Dict]
    # 원본 공지 목록
    # - text: 공지 원문
    # - link: 공지 링크
    # - date_posted: 공지 게시일
    
    extracted_facts: List[Dict]
    # 공지 수집자의 산출물
    # - notice_id: 식별자
    # - title: 제목
    # - targets: 대상 (예: "1~3학년 및 복수전공생")
    # - deadline: 마감일
    # - what_to_do: 해야 할 일
    # - materials: 준비물
    # - link: 신청 링크
    
    student_groups: List[str]
    # 정의된 학생 유형 (예: ["1학년", "4학년", "복수전공생", "교환학생 준비생"])
    
    grouped_tasks: Dict[str, List[Dict]]
    # 대상 분류자의 산출물
    # 예: {
    #   "1학년": [
    #     {"notice_title": "...", "action": "...", "deadline": "..."},
    #   ],
    #   "4학년": [...]
    # }
    
    draft_guide: Dict[str, str]
    # 안내 작성자의 산출물
    # 예: {
    #   "1학년": "**1학년이 해야 할 일**\n\n- [ ] 수강신청...",
    #   "4학년": "..."
    # }
    
    review_notes: List[Dict]
    # 검토자의 피드백
    # - group: 학생 유형
    # - issue: 문제점 (예: "원문에 없는 조건 추가")
    # - severity: 심각도 ("critical" / "warning" / "info")
    # - suggestion: 수정 제안
    
    human_feedback: Dict
    # 사람의 승인/수정
    # - approved: bool
    # - notes: 추가 수정 사항
    
    final_answer: Dict
    # 최종 안내문
```

---

## Step 5. 사람 승인 지점 정하기

### 승인 지점

**승인이 필요한 곳: 검토자 검토 완료 후**

```
검토자 → [여기서 사람이 최종 확인] → 게시
```

### 사람이 확인해야 하는 이유

- **마감일 오류**: 학생이 잘못된 날짜로 신청했다가 낙방할 수 있다
- **신청 자격 오류**: "4학년만" vs "4학년 제외" 같은 실수는 직접 피해를 입힌다
- **링크 오류**: 잘못된 신청 링크로 원치 않는 시스템에 접근할 수 있다
- **원문 왜곡**: 검토자도 놓칠 수 있는 미묘한 조건 변화 감지

### 승인 후 다음 단계

검토자가 승인 O → 최종 안내문을 학과 홈페이지/공지 채널 형식에 맞게 다듬음 → 게시

---

## Step 6. 실패 조건 쓰기

멀티에이전트 설계가 실패할 수 있는 경우:

1. **공지 수집자 오류 → 전체 파이프라인 오류**
   - 마감일을 "2024년 4월 30일"에서 "4월 30일"로 뽑으면, 연도 정보 손실
   - 대상이 "제외: 휴학생" 같은 부정형일 때 놓칠 수 있음

2. **대상 분류자의 모호한 판단**
   - 공지에 "3학년 이상"이라고 나오면, 4학년뿐만 아니라 대학원생도 포함인가?
   - 복수전공생이 수강신청 대상인데, 동시에 "복수전공은 별도 절차"라는 조건이 있을 때 혼동

3. **안내 작성자의 추측 추가**
   - 원문의 "수강신청"을 "수강신청(수강계획서 준비 필수)"로 과장할 수 있음
   - 검토자가 잡지 못할 경우 잘못된 정보가 학생에게 전달됨

4. **검토 없이 자동 게시**
   - 모든 에이전트가 완벽하다고 가정하고 사람 검증을 생략하면 원문에 없는 조건이 추가될 수 있음

5. **공지 1개 + 단순 전달인 경우**
   - "2024학년도 수강신청"이라는 공지 1개만 일괄 공지하는 경우
   - 이 경우 멀티에이전트 오히려 불필요하고, 단일 에이전트 + 요약이 더 빠름

---

## Step 7. 멀티에이전트를 쓰는 근거 정리

### 왜 이 작업을 멀티에이전트로 나누나?

1. **책임 분리가 명확함**
   - 공지 원본 처리 ≠ 분류 ≠ 안내문 작성 ≠ 최종 검증

2. **각 단계의 입력과 출력이 구분됨**
   - 공지 수집자의 출력(구조화된 표) = 대상 분류자의 입력
   - 안내문 작성자의 출력 = 검토자가 원본과 대조할 기준

3. **검증 루프가 자동화됨**
   - 검토자가 자기 산출물을 직접 검증하지 않음
   - 각 단계에서 독립적인 검증 기준이 있음

4. **사람이 개입할 지점이 분명함**
   - 검토 완료 후만 사람이 승인하면 됨
   - 중간 단계(사실 추출, 분류, 작성)는 에이전트가 담당

5. **동시 처리 가능**
   - 공지가 5개면, 공지 수집자는 5개를 병렬로 처리 가능
   - 각 학생 유형별 안내문을 동시에 작성 가능

---

## Step 8. 5문장 회고 쓰기

1. **작업의 본질**: 이 작업은 단순 요약이 아니라, 일관성 있는 정보 추출과 대상별 재구성이 핵심이다. 같은 마감일인데도 학년별로 신청 자격이 다를 수 있기 때문이다.

2. **역할 분리의 효과**: 역할을 나누었을 때 가장 분명해진 것은 사실(공지 원문의 명시적 정보)과 해석(학생 유형별 의미)의 분리다. 공지 수집자는 원문 그대로만, 분류자는 그 해석만 담당한다.

3. **핵심 공유 상태**: 가장 중요한 공유 상태는 `extracted_facts`(사실 목록)이다. 이것이 정확하지 않으면 이후 모든 단계가 틀린다.

4. **실패 조건의 중요성**: 공지 1개이거나 조건이 매우 단순하면(예: "모두 수강신청"), 멀티에이전트는 오히려 느리고 번거롭다. 이 경우 단일 에이전트로 충분하다.

5. **다음 주의 개선점**: 10주차에서 Supervisor 패턴을 쓴다면, 검토자가 "공지 수집자로 다시 돌아가"라고 명령할 수 있게 된다. 예를 들어 마감일 오류를 발견했을 때 공지 수집자에게 다시 검토하도록 요청할 수 있다. 이는 순차형에서는 불가능한 피드백 루프다.

---

## 자기 점검

- [x] 역할이 책임 단위로 나뉘었는가? → 각 역할의 입출력이 분명하다
- [x] 각 역할의 입력과 출력이 분명한가? → 공지 수집자: 원문 → 사실 표 / 분류자: 사실 표 → 유형별 목록 / 작성자: 유형별 목록 → 안내문 / 검토자: 안내문 + 원본 → 피드백
- [x] 사람 승인 지점이 필요한 곳에 있는가? → 검토자 완료 후 사람 승인
- [x] 멀티에이전트를 쓰지 않아도 되는 작업은 아닌가? → 공지가 여러 개이고 대상 조건이 복잡하면 적합
- [x] 다음 주 Supervisor 패턴으로 옮길 수 있는 흐름인가? → 검토자가 거절하면 어느 역할로 보낼지 결정 가능
