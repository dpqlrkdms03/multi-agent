# Todo (주차별 계획)

아래는 수업 기간(11주차~14주차)에 따라 나눈 작업 계획입니다. 각 주차는 해당 주차의 목표만 포함하고, 세부 구현은 주차 내에서 Copilot 또는 팀이 단계별로 진행합니다.

- **11주차 — 뼈대 만들기(골격)**
  - `schedule_agent.py`의 기본 함수 서명과 `main()` 흐름을 정의
  - 입력(`sample_notices.txt`) 로드, 공지 블록 분리 함수 구현
  - 출력 포맷(`output.md`, `output_user_guide.md`) 샘플 템플릿 작성

- **12주차 — 사실 추출**
  - `extract_facts()` 구현: 제목, 일시, 대상, 준비물, 주요행동 추출
  - 날짜 정규화(`normalize_date`) 및 간단한 검증 함수 추가
  - 추출 결과 샘플로 단위 테스트 실행

- **13주차 — 사용자별 안내문 작성**
  - `classify_target()`으로 대상 표준화
  - `compose_user_guides()`로 사용자별 요약문 생성
  - 사용자별 출력 예시(`output_user_guide.md`) 검토 및 개선

- **14주차 — 최종 정리 및 문서화**
  - 전체 흐름 통합, `python schedule_agent.py`로 회귀 테스트
  - `output.md`, `output_user_guide.md` 최종 포맷 확정
  - `README.md`에 실행 방법과 수업용 가이드 작성

각 주차의 작업은 수업 시간에 Copilot에게 지시한 뒤, 나온 결과를 검토하고 승인하면서 진행하세요.

## 12주차 개선 항목 (Assignment Priority Agent)

- 핵심 정보 추출 정확도 향상: `extract_facts()`의 정규식을 보완하고 `D-` 표기 외에 `YYYY-MM-DD` 같은 날짜 형식도 인식하도록 처리 계획 수립
- 분류 기준 안정화: 학년·범위 표기(예: `3~4학년`)와 경계값(D-2/D-3 등) 처리 규칙을 명문화하여 일관된 분류 확보
- 결과 파일 출력 추가: 실행 결과를 `output.md`로 그룹별 Markdown 형태로 저장하는 기능 추가(인코딩 UTF-8 명시)
- 외부 도구 검토: Groq API 또는 다른 외부 NLP 도구 도입의 장단점과 비용·보안 영향을 비교 검토
- 불확실성 처리 정책 마련: 입력에 없는 정보는 자동 추정하지 않고 `확인 필요`로 표기하는 정책 수립


- **12주차 개선 항목 — StudyCaffeine Agent 개선**
  - 핵심 정보 추출을 더 정확하게 만들기:
    - `extract_facts()`의 정규식 및 파싱 로직 정리(카페인 mg, 섭취시각, 집중도 수치, 수면시간 등).
    - 다양한 시간/단위 표기(예: "오전 9시", "21:30", "9:30 PM")와 약어(D-표기, am/pm) 정규화 처리 추가.
    - 경계 케이스(숫자 누락, 오타) 감지 시 `확인 필요` 태그 달기.
  - 분류 기준을 더 안정적으로 만들기:
    - `classify_items()`의 임계값과 라벨 규칙(건강형/주의형/위험형)을 중앙화된 설정으로 분리.
    - 여러 샘플을 이용한 단위 테스트로 경계값 안정성 검증 절차 추가.
    - 다중 입력(하루 복수 섭취)과 누락 데이터 처리 규칙 문서화.
  - 결과를 `output.md` 파일로 저장하기:
    - 실행 시 결과를 UTF-8 인코딩의 `output.md`로 덧붙이기(append) 또는 덮어쓰기 옵션 추가.
    - 파일 포맷: 개인별 요약 + 전반적 통계(평균 카페인 mg, 평균 집중도, 평균 수면시간).
    - 저장 시 타임스탬프와 실행 파라미터를 헤더로 기록.
  - Groq API 또는 외부 도구 도입 여부 결정:
    - 외부 NLP(예: Groq) 도입 시 기대 효과(추출 정확도 향상)와 단점(비용, 개인정보 영향, 의존성) 비교표 작성.
    - PoC(작은 샘플)로 성능/비용 측정 후 도입 결정 권장.
  - 입력에 없는 내용을 단정하지 않도록 검토하기:
    - 필드가 없는 경우 자동 추정 금지 — `확인 필요`로 표기하고 후속 입력/검증 절차 권장.
    - 사용자 메시지 또는 UI/출력에 불확실성 표시(신뢰도 점수 또는 플래그) 추가.
