# 12주차 과제

## GitHub 저장소
- URL: `여기에 본인 GitHub 저장소 URL`

## 내 에이전트
- 에이전트 이름: StudyCaffeine Agent
- 에이전트 유형: 의사결정 보조형, 정보 추출형
- 해결하려는 문제: 사용자가 자신의 카페인 섭취 습관이 집중도와 수면에 어떤 영향을 미치는지 파악하기 어렵다. 카페인 섭취 기록, 집중도, 수면 기록을 분석하여 학습 효율과 수면 습관 개선을 돕고자 한다.
- 입력 자료: 카페인 섭취량, 마지막 섭취 시간, 집중도 점수, 수면 시간
- 결과 파일: `output.md`, `caffeine_report.md`

## 중간 정보 구조
| key | 의미 |
|---|---|
| `caffeine_amount` | 카페인 섭취량(정수, 잔 단위) |
| `last_caffeine_time` | 마지막 섭취 시간(`HH:MM` 형식) |
| `focus_score` | 집중도 점수(0~100) |
| `sleep_hours` | 수면 시간(시간 단위) |
| `sleep_status` | 수면 상태(`충분` / `부족`) |
| `caffeine_risk` | 카페인 위험도(`낮음` / `보통` / `높음`) |

## 판단/처리 기준
- 기준 1: 마지막 섭취 시간이 `18:00` 이후이고 수면 시간이 6시간 미만이면 `위험형`으로 분류한다.
- 기준 2: 카페인 섭취량이 많거나(`4잔 이상`) 집중도가 낮거나(`75 미만`) 수면이 부족하면 `주의형`으로 분류한다.
- 기준 3: 위 조건에 해당하지 않으면 `건강형`으로 분류한다.

## 실행 명령
- `python my_agent.py`

## 실행 결과 요약
- 입력 항목 개수: 3개
- 만들어진 중간 정보: 3개 레코드
- 판단/처리 결과: 건강형 2개, 위험형 1개
- 생성된 파일: `output.md`, `caffeine_report.md`

## 오늘 수정한 함수
| 함수 | 역할 | 수정 내용 |
|---|---|---|
| `extract_facts` | 입력 분석 | 카페인 섭취량, 섭취 시간, 집중도, 수면 시간을 추출하고 `sleep_status`, `caffeine_risk`를 생성하도록 개선 |
| `classify_items` | 판단/처리 | 추출된 중간 정보를 바탕으로 건강형/주의형/위험형을 간단한 조건으로 분류 |
| `save_result` | 결과 저장 | 분류 결과를 Markdown 형식으로 `caffeine_report.md`에 저장하도록 추가 |
| `save_markdown_table` | 결과 저장 | 중간 정보 리스트를 Markdown 표로 `output.md`에 저장하도록 추가 |
| `analyze_input` | 입력 분석 연결 | 규칙 기반 분석과 LLM 보조 분석을 분기하도록 구성 |
| `fetch_external_context` | 외부 자원 연결 | `caffeine_report.md` 또는 `caffeine_log.csv` 존재 여부를 확인하고 최근 기록을 읽어오도록 추가 |

## LLM 또는 외부 도구
- LLM 보조 사용: 가능하도록 구조만 추가했고, 기본값은 `False`
- 외부 도구/API/파일: `caffeine_report.md`를 우선 연결하고, 없으면 `caffeine_log.csv`를 확인
- fallback 동작: `GROQ_API_KEY`가 없거나 Groq 호출에 실패하면 규칙 기반 분석으로 자동 전환

## 코딩에이전트에게 준 지시 2개
1. 입력 분석 함수는 규칙 기반으로 유지하되, 반환 형식을 다음 단계가 쓰기 쉬운 dict list로 정리해 달라고 요청했다.
2. 결과 저장 함수는 Markdown 파일로 저장하고, 실행 결과를 사람이 확인하기 쉽게 출력해 달라고 요청했다.

## 남은 문제
- [ ] `GitHub 저장소 URL` 최종 입력
- [ ] 필요하면 `caffeine_log.csv` 실제 누적 파일까지 추가 연결
- [ ] 필요하면 Groq 실제 사용 여부를 더 명확히 정리